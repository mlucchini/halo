import React from 'react'
import { storiesOf } from '@kadira/storybook'
import Stars from '../components/stars'
import Sky from '../components/sky'
import Landscape from '../components/landscape'
import Landfront from '../components/landfront'
import Stag from '../components/stag'
import Sun from '../components/sun'
import Clouds from '../components/clouds'
import Vignette from '../components/vignette'
import Twinkles from '../components/twinkles'

storiesOf('Clouds', module)
  .add('default', () => (
    <Clouds />
  ))

storiesOf('Stars', module)
  .add('default', () => (
    <Stars />
  ))

storiesOf('Sky', module)
  .add('default', () => (
    <Sky />
  ))

storiesOf('Landscape', module)
  .add('default', () => (
    <Landscape />
  ))

storiesOf('Landfront', module)
  .add('default', () => (
    <Landfront />
  ))

storiesOf('Stag', module)
  .add('default', () => (
    <Stag />
  ))

storiesOf('Sun', module)
  .add('default', () => (
    <Sun />
  ))

storiesOf('Clouds', module)
  .add('default', () => (
    <Clouds />
  ))

storiesOf('Vignette', module)
  .add('default', () => (
    <Vignette />
  ))

storiesOf('Twinkles', module)
  .add('default', () => (
    <Twinkles />
  ))
